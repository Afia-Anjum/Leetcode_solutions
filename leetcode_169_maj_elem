from collections import Counter
class Solution:
    def majorityElement(self, nums: List[int]) -> int:
        number_of_elements=len(nums)
        maj_element_cnt=number_of_elements/2
        l=Counter(nums)
        print(l)
        for key in l.keys():
            print(key)
            if l.get(key)>maj_element_cnt:
                return key
        

